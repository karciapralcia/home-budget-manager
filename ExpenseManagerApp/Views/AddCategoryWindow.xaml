<Window x:Class="ExpenseManagerApp.Views.AddCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExpenseManagerApp.Views"
        mc:Ignorable="d"
        Title="AddCategoryWindow" Height="150" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <!-- Label and TextBox for Description -->
        <TextBlock Text="Name of category:" Grid.Column="0" VerticalAlignment="Center" Margin="4,0,1,0" Height="20"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewCategory}" Margin="2,10,2,10"/>
        <Button Content="Add category" 
            Margin="85,15,0,14" HorizontalAlignment="Left" Command="{Binding AddCategoryCommand}" Grid.ColumnSpan="2" Grid.Row="1" Width="80"/>
    </Grid>
</Window>

